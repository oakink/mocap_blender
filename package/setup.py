from setuptools import setup

setup(
    name='mocap_blender',
    version='0.1.0',
    packages=['mocap_blender', 'data_source', 'render_scene'],
    author='oakink',
    description='mocap_blender',
    install_requires=[],
    tests_require=[]
)
